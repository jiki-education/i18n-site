---
lang: "fa"
type: "exercise"
slug: "relational-sun"
title: "خورشید نسبی"
status: "published"
source_repo: "front-end"
source_path: "curriculum/src/exercises/relational-sun"
en_md5: "9ff58522efc81e1fab45e94feaa23998"
governance_sha: "c80036b"
content_version: "c9d1e0fb5f58"
published_at: "2026-08-06"
messages: [{"heading":"Messages shown in the exercise","namespaces":[{"name":"checks","rows":[{"key":"wrongPosition","english":"The sun is either in the wrong place or the wrong size. Check your `sunX` and `sunY` calculations, and make sure you're passing `radius` to `circle`.","target":"خورشید یا در جای اشتباهی قرار دارد یا اندازه‌اش اشتباه است. محاسبات `sunX` و `sunY` را بررسی کنید و مطمئن شوید که `radius` را به `circle` می‌دهید."},{"key":"notResponsive","english":"Your code doesn't scale correctly when `gap` is {{gap}} and `radius` is {{radius}}. `sunX` and `sunY` should be derived from `gap` and `radius`, not hardcoded numbers.","target":"کد شما وقتی `gap` برابر با {{gap}} و `radius` برابر با {{radius}} است درست مقیاس‌بندی نمی‌شود. `sunX` و `sunY` باید از `gap` و `radius` محاسبه شوند، نه اعداد هاردکد شده."}]},{"name":"tasks","rows":[{"key":"positionSun.name","english":"Position the sun","target":"خورشید را قرار دهید"},{"key":"positionSun.description","english":"Derive `sunX` and `sunY` from `canvasSize`, `gap`, and `radius` so the sun scales correctly.","target":"`sunX` و `sunY` را از `canvasSize`، `gap` و `radius` به‌دست آورید تا خورشید به‌درستی مقیاس‌بندی شود."}]},{"name":"scenarios","rows":[{"key":"positionSun.name","english":"Position the sun","target":"خورشید را قرار دهید"},{"key":"positionSun.description","english":"Derive the sun's position from `gap` and `radius` so it scales.","target":"موقعیت خورشید را از `gap` و `radius` به‌دست آورید تا مقیاس‌بندی شود."}]},{"name":"hints","rows":[{"key":"sunXPosition.question","english":"How should I think about the sun's x position?","target":"چطور باید به موقعیت x خورشید فکر کنم؟"},{"key":"sunXPosition.answer","english":"Picture the right edge of the canvas. The edge of the sun sits `gap` to the left of that edge. The *centre* of the sun is one more radius further in than that.","target":"لبه‌ی راست بوم را تصور کنید. لبه‌ی خورشید `gap` به سمت چپ آن لبه قرار دارد. *مرکز* خورشید یک شعاع دیگر از آن به سمت چپ فاصله دارد."},{"key":"sunYPosition.question","english":"How should I think about the sun's y position?","target":"چطور باید به موقعیت y خورشید فکر کنم؟"},{"key":"sunYPosition.answer","english":"It's the same idea as the x position, but measured down from the top edge of the canvas instead of in from the right edge.","target":"فکرش مثل موقعیت x است، با این تفاوت که این بار فاصله از لبه‌ی بالایی بوم به سمت پایین حساب می‌شود، نه از لبه‌ی راست به سمت چپ."},{"key":"whyNotHardcode.question","english":"Why can't I just write the numbers directly?","target":"چرا نمی‌توانم اعداد را مستقیم بنویسم؟"},{"key":"whyNotHardcode.answer","english":"If you hardcode `sunX` and `sunY`, the sun will stay in one place when `gap` or `radius` change. Building them from `gap` and `radius` keeps the sun in the corner no matter what values you choose.","target":"اگر `sunX` و `sunY` را هاردکد کنید، خورشید با تغییر `gap` یا `radius` در یک جا ثابت می‌ماند. ساختن آن‌ها از `gap` و `radius` باعث می‌شود خورشید بدون توجه به مقادیری که انتخاب می‌کنید، در گوشه بماند."}]},{"name":"functions","rows":[{"key":"circle.description","english":"Draw a circle centered at (centerX, centerY) with the given radius and color","target":"دایره‌ای رسم می‌کند که مرکزش در (centerX، centerY) است، با شعاع و رنگ داده‌شده."},{"key":"circle.category","english":"Drawing Shapes","target":"رسم اشکال"}]}]},{"heading":"Shared \"draw\" messages","note":"Shared by every exercise in the draw category, not just this one. A change here changes all of them.","namespaces":[{"name":"errors","rows":[{"key":"allInputsNumbers","english":"All inputs must be numbers","target":"همه‌ی ورودی‌ها باید عدد باشند"},{"key":"blueRange","english":"Blue must be between 0 and 255","target":"مقدار آبی باید بین ۰ و ۲۵۵ باشد"},{"key":"circleInputsNumbers","english":"The x, y, and radius inputs must be numbers","target":"ورودی‌های x، y و radius باید عدد باشند"},{"key":"colorNamedOrHex","english":"Color must be a named color (\"orange\", \"blue\", \"white\", etc) specified in the instructions, or a hex color starting with #","target":"رنگ ورودی باید یکی از رنگ‌هایی باشد که در توضیحات تمرین گفتیم (\"orange\"، \"blue\"، \"white\" و مانند آن‌ها)، یا یک رنگ «هگز» باشد (اعداد نشان‌دهنده‌ی رنگ در هگز با # شروع می‌شوند)"},{"key":"colorNotAllowed","english":"Color should not be specified for this exercise","target":"در این تمرین رنگ را مشخص نکنید"},{"key":"colorNotString","english":"Color must be a string","target":"رنگ باید یک رشته باشد"},{"key":"ellipseInputsNumbers","english":"The x, y, rx, and ry inputs must be numbers","target":"ورودی‌های x، y، rx و ry باید عدد باشند"},{"key":"greenRange","english":"Green must be between 0 and 255","target":"مقدار سبز باید بین ۰ و ۲۵۵ باشد"},{"key":"heightPositive","english":"Height must be greater than 0","target":"ارتفاع باید بزرگ‌تر از ۰ باشد"},{"key":"hueRange","english":"Hue must be between 0 and 360","target":"مقدار فام باید بین ۰ و ۳۶۰ باشد"},{"key":"lightnessRange","english":"Lightness must be between 0 and 100","target":"مقدار روشنایی باید بین ۰ و ۱۰۰ باشد"},{"key":"lineInputsNumbers","english":"The x1, y1, x2, and y2 inputs must be numbers","target":"ورودی‌های x1، y1، x2 و y2 باید عدد باشند"},{"key":"rectInputsNumbers","english":"The x, y, width, and height inputs must be numbers","target":"ورودی‌های x، y، width و height باید عدد باشند"},{"key":"redRange","english":"Red must be between 0 and 255","target":"مقدار قرمز باید بین ۰ و ۲۵۵ باشد"},{"key":"saturationRange","english":"Saturation must be between 0 and 100","target":"مقدار اشباع باید بین ۰ و ۱۰۰ باشد"},{"key":"triangleInputsNumbers","english":"The x1, y1, x2, y2, x3, and y3 inputs must be numbers","target":"ورودی‌های x1، y1، x2، y2، x3 و y3 باید عدد باشند"},{"key":"widthPositive","english":"Width must be greater than 0","target":"عرض باید بزرگ‌تر از ۰ باشد"}]},{"name":"describers","rows":[{"key":"rectangle.default","english":"drew a rectangle at coordinates (${arg1}, ${arg2}) with a width of ${arg3}, a height of ${arg4}, and a color of ${arg5}","target":"مستطیلی در مختصات (${arg1}، ${arg2}) با عرض ${arg3}، ارتفاع ${arg4} و رنگ ${arg5} رسم شد"},{"key":"rectangle.fixedColor","english":"drew a rectangle at coordinates (${arg1}, ${arg2}) with a width of ${arg3} and a height of ${arg4}","target":"مستطیلی در مختصات (${arg1}، ${arg2}) با عرض ${arg3} و ارتفاع ${arg4} رسم شد"},{"key":"triangle.default","english":"drew a triangle with three points: (${arg1}, ${arg2}), (${arg3}, ${arg4}), and (${arg5}, ${arg6}) with a color of ${arg7}","target":"مثلثی با سه نقطه‌ی (${arg1}، ${arg2})، (${arg3}، ${arg4}) و (${arg5}، ${arg6}) و رنگ ${arg7} رسم شد"},{"key":"triangle.fixedColor","english":"drew a triangle with three points: (${arg1}, ${arg2}), (${arg3}, ${arg4}), and (${arg5}, ${arg6})","target":"مثلثی با سه نقطه‌ی (${arg1}، ${arg2})، (${arg3}، ${arg4}) و (${arg5}، ${arg6}) رسم شد"},{"key":"circle.default","english":"drew a circle with its center at (${arg1}, ${arg2}), a radius of ${arg3}, and a color of ${arg4}","target":"دایره‌ای با مرکز (${arg1}، ${arg2})، شعاع ${arg3} و رنگ ${arg4} رسم شد"},{"key":"circle.fixedColor","english":"drew a circle with its center at (${arg1}, ${arg2}) and a radius of ${arg3}","target":"دایره‌ای با مرکز (${arg1}، ${arg2}) و شعاع ${arg3} رسم شد"},{"key":"ellipse.default","english":"drew an ellipse with its center at (${arg1}, ${arg2}), a horizontal radius of ${arg3}, a vertical radius of ${arg4}, and a color of ${arg5}","target":"بیضی‌ای با مرکز (${arg1}، ${arg2})، شعاع افقی ${arg3}، شعاع عمودی ${arg4} و رنگ ${arg5} رسم شد"},{"key":"ellipse.fixedColor","english":"drew an ellipse with its center at (${arg1}, ${arg2}), a horizontal radius of ${arg3}, and a vertical radius of ${arg4}","target":"بیضی‌ای با مرکز (${arg1}، ${arg2})، شعاع افقی ${arg3} و شعاع عمودی ${arg4} رسم شد"},{"key":"line.default","english":"drew a line from (${arg1}, ${arg2}) to (${arg3}, ${arg4}) with a color of ${arg5}","target":"خطی از (${arg1}، ${arg2}) تا (${arg3}، ${arg4}) با رنگ ${arg5} رسم شد"},{"key":"line.fixedColor","english":"drew a line from (${arg1}, ${arg2}) to (${arg3}, ${arg4})","target":"خطی از (${arg1}، ${arg2}) تا (${arg3}، ${arg4}) رسم شد"},{"key":"clear","english":"cleared the canvas","target":"الان بوم خالی است"},{"key":"hsl","english":"converted HSL color (hue: ${arg1}, saturation: ${arg2}, lightness: ${arg3}) to a hex string","target":"رنگ HSL (فام: ${arg1}، اشباع: ${arg2}، روشنایی: ${arg3}) به یک رشته‌ی هگز تبدیل شد"},{"key":"rgb","english":"converted RGB color (red: ${arg1}, green: ${arg2}, blue: ${arg3}) to a hex string","target":"رنگ RGB (قرمز: ${arg1}، سبز: ${arg2}، آبی: ${arg3}) به یک رشته‌ی هگز تبدیل شد"}]}]}]
---

در این تمرین، وظیفه‌ی شما این است که خورشیدی را در گوشه‌ی بالا-راست آسمان با استفاده از محاسبات و متغیرها قرار دهید. محدودیت اصلی این است که لبه‌ی خورشید همیشه باید یک **فاصله‌ی** ثابت از لبه‌ی بوم دور باشد، صرف‌نظر از اندازه‌ی خورشید.

ما چهار «متغیر» را در بالای فایل برایتان تعریف کرده‌ایم:

- `canvasSize` (اندازه‌ی بوم): اندازه‌ی بوم که برابر با `100` است.
- `color` (رنگ): برابر با `"yellow"` تنظیم شده است.
- `gap` (فاصله): فاصله‌ی لبه‌ی خورشید از لبه‌های بالا و راست بوم.
- `radius` (شعاع): شعاع خورشید.

### یک فرایند دو مرحله‌ای

برای حل این تمرین، دو کار انجام دهید:

#### ۱. تعریف متغیرهای محاسبه‌شده

متغیرهای `sunX` (مختصات x خورشید) و `sunY` (مختصات y خورشید) را تعریف کنید که با استفاده از `gap`، `radius` و `canvasSize` مرکز خورشید را در گوشه‌ی بالا-راست مشخص می‌کنند.

#### ۲. رسم دایره

دایره‌ای را با استفاده از `sunX`، `sunY`، `radius` و `color` رسم کنید.

## انعطاف‌پذیری

به‌طور پیش‌فرض، مقدار `gap` برابر با ۱۰ و `radius` برابر با ۱۵ تنظیم شده است. ابتدا تمرین را با همین مقادیر حل کنید تا بتوانید از پس‌زمینه برای تنظیم موقعیت استفاده کنید. وقتی کدتان درست باشد، دایره را در جای درست صفحه مشاهده خواهید کرد.

با این حال، برای قبولی در تمرین نمی‌توانید صرفاً مقدار را «هاردکد» کنید (یعنی یک عدد ثابت بگذارید)؛ متغیرهای `sunX` و `sunY` باید محاسباتی باشند که شامل متغیرهای دیگر هستند.

بعد از قبولی در تمرین، روی «کدم را مرتب کن» کلیک کنید و مقادیر `gap` و `radius` را در بالای فایل تغییر دهید و کدتان را دوباره اجرا کنید تا ببینید خورشید بزرگ‌تر می‌شود یا جابه‌جا می‌شود، در حالی که هم‌چنان در گوشه می‌ماند.

وقتی کارتان تمام شد، می‌توانید روی «داشبورد» در بالای سمت راست کلیک کنید تا طبق معمول ادامه دهید.
